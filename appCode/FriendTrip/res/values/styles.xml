<resources>
    <style name="fragment_text">
        <item name="android:textColor">#ffacacac</item>
        <item name="android:textSize">25sp</item>
    </style>
    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    
    <!-- 退出的自定义dialog -->
    <style name="customExitDailogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/exit_dialog_bg</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>   
    
     <declare-styleable name="CircleImageView">
        <attr name="border_width" format="dimension" />
        <attr name="border_color" format="color" />
    </declare-styleable>
    
    <!-- 选择日期格式 -->
    <style name="FullHeightDialog" parent="android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/transparent</item>
    </style>
    
    <style name="AnimationPreview">
        <item name="android:windowEnterAnimation">@anim/more_in</item>
        <item name="android:windowExitAnimation">@anim/more_out</item>
    </style>
    
  
    
     
     <style name="myProgressBarStyle" >
　　<item name="android:indeterminateDrawable">@drawable/rotate_draw</item>
　　<item name="android:minWidth">@dimen/dp70</item>
　　<item name="android:maxWidth">@dimen/dp70</item>
　　<item name="android:minHeight">@dimen/dp70</item>
　　<item name="android:maxHeight">@dimen/dp70</item>
</style>
</resources>
